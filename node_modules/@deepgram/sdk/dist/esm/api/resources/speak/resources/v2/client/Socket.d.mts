import * as core from "../../../../../../core/index.mjs";
import type * as Deepgram from "../../../../../index.mjs";
export declare namespace V2Socket {
    interface Args {
        socket: core.ReconnectingWebSocket;
    }
    type Response = string | Deepgram.speak.SpeakV2Connected | Deepgram.speak.SpeakV2SpeechStarted | Deepgram.speak.SpeakV2SpeechMetadata | Deepgram.speak.SpeakV2SpeechInterrupted | Deepgram.speak.SpeakV2Flushed | Deepgram.speak.SpeakV2SessionMetadata | Deepgram.speak.SpeakV2ConfigureSuccess | Deepgram.speak.SpeakV2ConfigureFailure | Deepgram.speak.SpeakV2Warning | Deepgram.speak.SpeakV2Error;
    type EventHandlers = {
        open?: () => void;
        message?: (message: Response) => void;
        close?: (event: core.CloseEvent) => void;
        error?: (error: Error) => void;
    };
}
export declare class V2Socket {
    readonly socket: core.ReconnectingWebSocket;
    protected readonly eventHandlers: V2Socket.EventHandlers;
    private handleOpen;
    private handleMessage;
    private handleClose;
    private handleError;
    constructor(args: V2Socket.Args);
    /** The current state of the connection; this is one of the readyState constants. */
    get readyState(): core.ReconnectingWebSocket.ReadyState;
    /**
     * @param event - The event to attach to.
     * @param callback - The callback to run when the event is triggered.
     * Usage:
     * ```typescript
     * this.on('open', () => {
     *     console.log('The websocket is open');
     * });
     * ```
     */
    on<T extends keyof V2Socket.EventHandlers>(event: T, callback: V2Socket.EventHandlers[T]): void;
    sendSpeak(message: Deepgram.speak.SpeakV2Speak): void;
    sendFlush(message: Deepgram.speak.SpeakV2Flush): void;
    sendInterrupt(message: Deepgram.speak.SpeakV2Interrupt): void;
    sendConfigure(message: Deepgram.speak.SpeakV2Configure): void;
    sendClose(message: Deepgram.speak.SpeakV2Close): void;
    /** Connect to the websocket and register event handlers. */
    connect(): V2Socket;
    /** Close the websocket and unregister event handlers. */
    close(): void;
    /** Returns a promise that resolves when the websocket is open. */
    waitForOpen(): Promise<core.ReconnectingWebSocket>;
    /** Asserts that the websocket is open. */
    private assertSocketIsOpen;
    /** Send a binary payload to the websocket. */
    protected sendBinary(payload: ArrayBuffer | Blob | ArrayBufferView): void;
    /** Send a JSON payload to the websocket. */
    protected sendJson(payload: Deepgram.speak.SpeakV2Speak | Deepgram.speak.SpeakV2Flush | Deepgram.speak.SpeakV2Interrupt | Deepgram.speak.SpeakV2Configure | Deepgram.speak.SpeakV2Close): void;
}
