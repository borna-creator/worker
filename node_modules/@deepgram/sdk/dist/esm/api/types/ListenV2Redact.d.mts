/** Redaction removes sensitive information from your transcripts. On Flux, only `numbers` and `aggressive_numbers` are supported. */
export declare const ListenV2Redact: {
    readonly Numbers: "numbers";
    readonly AggressiveNumbers: "aggressive_numbers";
};
export type ListenV2Redact = (typeof ListenV2Redact)[keyof typeof ListenV2Redact] | string;
