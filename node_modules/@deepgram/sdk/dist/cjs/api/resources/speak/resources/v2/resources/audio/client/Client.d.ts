import type { BaseClientOptions, BaseRequestOptions } from "../../../../../../../../BaseClient.js";
import { type NormalizedClientOptionsWithAuth } from "../../../../../../../../BaseClient.js";
import * as core from "../../../../../../../../core/index.js";
import * as Deepgram from "../../../../../../../index.js";
export declare namespace AudioClient {
    type Options = BaseClientOptions;
    interface RequestOptions extends BaseRequestOptions {
    }
}
export declare class AudioClient {
    protected readonly _options: NormalizedClientOptionsWithAuth<AudioClient.Options>;
    constructor(options?: AudioClient.Options);
    /**
     * Synthesize a complete block of text into a single audio response using Deepgram's Flux TTS batch (REST) API. Use this for pre-rendering fixed audio (IVR prompts, notifications, narration) where the whole text is known up front and you don't need incremental playback or interruption.
     *
     * @throws {@link Deepgram.BadRequestError}
     */
    generate(request: Deepgram.speak.v2.SpeakV2Request, requestOptions?: AudioClient.RequestOptions): core.HttpResponsePromise<core.BinaryResponse>;
    private __generate;
}
