import type * as Deepgram from "../../../../../index.js";
export interface SpeakV2ConfigureSuccess {
    /** Message type identifier */
    type: "ConfigureSuccess";
    /** Synthesis configuration. A field is present only when it has been set on this session. */
    applied: SpeakV2ConfigureSuccess.Applied;
}
export declare namespace SpeakV2ConfigureSuccess {
    /**
     * Synthesis configuration. A field is present only when it has been set on this session.
     */
    interface Applied {
        speed?: Deepgram.SpeakV2Speed | undefined;
    }
}
